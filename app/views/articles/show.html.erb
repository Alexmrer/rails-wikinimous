<h1> <%= "#{@article.title}" %>
<%= link_to "✏️", edit_article_path() %>
     <%= link_to "🗑", article_path(@article), method: :delete, data: { confirm: "Are you sure you want to delete #{@article.title}?" }%>
</h1>
<p><%= link_to "Back to All Posts", articles_path %></p>
